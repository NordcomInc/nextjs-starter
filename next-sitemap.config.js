/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: `https://nextjs-starter.nordcom.io`,

    generateRobotsTxt: true,
    generateIndexSitemap: true
};

export default config;
